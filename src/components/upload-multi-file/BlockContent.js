import { Typography } from '@mui/material';

export default function BlockContent() {
    return (
        <Typography variant="body2" color="textSecondary">
            드래그 앤 드롭 또는 파일 선택을 통해 이미지를 업로드하세요
        </Typography>
    );
}
